{
  "name": "polardbx-zero-action",
  "version": "1.0.0",
  "description": "GitHub Action to create ephemeral PolarDB-X database instances for CI testing",
  "private": true,
  "scripts": {
    "build": "ncc build src/main.js -o dist/main && ncc build src/post.js -o dist/post"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  },
  "license": "Apache-2.0"
}
